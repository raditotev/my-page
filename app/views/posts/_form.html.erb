<%= simple_form_for(@post) do |f| %>

  <%= f.input :title %>
  <%= f.input :content, as: :ckeditor, input_html: { ckeditor: { toolbar: 'Pure' } } %>
  <%= f.input :all_tags %>

  <%= f.button :submit %>
<% end %>
