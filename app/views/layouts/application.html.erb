<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? "#{yield(:title)} | Radi's Page" : "Radi's Page" %></title>

  <%= stylesheet_link_tag "application" %>
  <link href='https://fonts.googleapis.com/css?family=Istok+Web|Lora' rel='stylesheet' type='text/css'>
  <link rel="icon" type="image/x-icon" href="<%= image_url "favicon.ico" %>">
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
<% if content_for?(:id) %>
<div id="<%= yield :id %>-banner">
  <div class="row">
  <h1><%= content_for?(:heading) ? yield(:heading) : yield(:title) %></h1>
  <h5><%= yield :subtitle %></h5>
  </div>
</div>
<% end %>

  <!-- Navigation -->
  <div class="title-bar <%= content_for?(:id) ? yield(:id) : "navbar" %>" data-responsive-toggle="menu" data-hide-for="medium">
    <button class="menu-icon" type="button" data-toggle></button>
    <div class="title-bar-title">Menu</div>
  </div>

  <div class="top-bar <%= content_for?(:id) ? yield(:id) : "navbar" %>" id="menu">
    <div class="row">
      <div class="medium-6 medium-centered text-center">
          <ul class="menu">
            <li><%= link_to "About", about_path %></li>
            <li><%= link_to "Portfolio", portfolio_path %></li>
            <li><%= link_to "Blog", blog_path %></li>
            <li><%= link_to "Contact", contact_path %></li>
          </ul>
      </div>
    </div>
  </div>
  <!-- Error messages -->
  <div class="row">
    <div class="small-8 small-centered medium-4 medium-centered">
      <div id="notice" class="text-center">
        <%= render 'shared/flash_messages' %>
      </div>
    </div>
  </div>

  <%= yield %>

</body>
</html>
